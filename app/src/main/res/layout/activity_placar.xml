<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="br.com.fiap.myapplication.PlacarActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            style="@style/elemento_placar_titulo"
            tools:text="TIME A"
            android:id="@+id/tvTimeCasa" />

        <TextView
            style="@style/elemento_placar_titulo"

            tools:text="TIME B"
            android:id="@+id/tvTimeVisitante" />
    </LinearLayout>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <TextView
            style="@style/elemento_placar_resultado"
            tools:text="0"
            android:id="@+id/tvPlacarCasa" />

        <TextView
            style="@style/elemento_placar_resultado"

            tools:text="0"
            android:id="@+id/tvPlacarVisitante" />
    </LinearLayout>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <Button style="@style/elemento_placar_botao" android:text="Gol"
            android:id="@+id/btGolCasa"
            android:onClick="golCasa" />
        <Button style="@style/elemento_placar_botao" android:text="Gol"
            android:id="@+id/btGolVisitante"
            android:onClick="golVisitante" />
    </LinearLayout>

</LinearLayout>
